# Checkpoint 04012026_2325

- Normalized trade timestamps (parse + last_trade_ts) to UTC-aware before comparisons in trade ingestion.
- Tests run: `uv run ruff format .`, `uv run ruff check .`, `uv run pytest`.
