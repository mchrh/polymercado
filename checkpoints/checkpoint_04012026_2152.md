# Checkpoint 04012026_2152

- Bootstrap Gamma sync on startup when no markets exist, so UI isn't empty on first run.
- Wrapped bootstrap in warning-only handler to avoid startup failure on network issues.
