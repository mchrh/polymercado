# Checkpoint 05012026_2337

## What changed
- Trade alerts now include the traded outcome (e.g., `Yes`/`No`) plus BUY/SELL and price in the human-facing message formatting.
- GUI tables now surface the same information:
  - Alerts page includes a message column (uses the same formatter as Telegram/Slack).
  - Whale Tape shows `side` and `outcome` columns.

## Files touched
- `src/polymercado/alerts/dispatcher.py` (trade message formatting)
- `src/polymercado/web/routes.py` (pass formatted message to alerts view)
- `src/polymercado/web/templates/alerts.html` (render message column)
- `src/polymercado/web/templates/whales.html` (render side/outcome columns)
- `tests/unit/test_alert_formatting.py` (new coverage)

## Checks run
- `uv run ruff format .`
- `uv run ruff check .`
- `uv run pytest`
